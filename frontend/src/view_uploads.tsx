import React,{useState} from "react";

interface SimpleDialogProps{
    open: boolean;
    setOpen: React.Dispatch<React.SetStateAction<boolean>>;
}

export default function ViewUpload({open,viewfile,setdialog}) {
    return(
      alert("hello")
    )

}